"use client";

const ButtonProjects = ({ target }) => (
  <a href="/dashboard" className="btn btn-primary">
    <button target={target}>Go to Projects</button>
  </a>
);

export default ButtonProjects;
